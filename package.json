{
  "name": "script-wrap-template",
  "description": "Template for wraps written in an interpreted language",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "npx polywrap build --no-codegen",
    "deploy": "npx polywrap deploy",
    "test": "cargo test"
  },
  "devDependencies": {
    "@types/jest": "27.0.3",
    "jest": "26.6.3",
    "polywrap": "0.10.3",
    "ts-jest": "26.5.4",
    "typescript": "4.0.7"
  }
}
